Commands. <target> is a folder that contains INDEX.target file

alsa-targetgen build <target> -- Builds dockerfile for image to build the target

every .target file can contain following "commands":

Target file is costructed as following:
It is a json file that are an array []
And every command is provided like that
{
    "name": "COMMAND_NAME",
    "args": [ command args ( Has to be provided as a string even if it's a boolean ) ]
}

TARGET <name> - Required gives target a name (Docker image name is alsa-targetgen-target-<name>)
TARGET_ENV <base_image> - Required. Docker base image (supported ubuntu_22 (22.04), ubuntu_24 (24.04))
TARGET_ENV_SETWORKFOLDER <folder name> - Change docker WORKDIR name (recomended APP)
Optional:
TARGET_ENV_APT <package> - APT Package to install on image
TARGET_ENV_COPY <local target name> <local target path in target> - Copy local object to docker image (To copy .sh files that you plan to run on image use COPYE)
TARGET_ENV_INITFILE <sh file name in target> - Set default sh file on image
TARGET_ENV_SHAREFOLDER <local folder name> <target folder name> - Add access to folder from docker (-v flag)
