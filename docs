Commands. <target> is a folder that contains INDEX.target file

alsa-targetgen build <target> -- Builds docker image to build the target
alsa-targetgen run <target> -- Runs build process with pre-builded (Using build <target>) image (user must be in docker group. App will refuse to run anything from root)
alsa-targetgen exec <target> -- Executes program if .target file allows it

every .target file can contain following "commands" (Commands begins with $ sign)

TARGET <name> - Required gives target a name (Docker image name is alsa-targetgen-target-<name>)
TARGET_REBUILD <true/false> - Required. Should the docker image deleted after running?
TARGET_ALLOW_RUN <true/false> - Required. Allow project to be runned? (execs ./output/target_run.sh)
TARGET_ENV <base_image> - Required. Docker base image (supported ubuntu_22 (22.04), ubuntu_24 (24.04))
TARGET_ENV_APT <package, [packages]> - APT Packages to install on image
Optional:
TARGET_ENV_SETWORKFOLDER <folder name> - (Not recomended to change). Change docker WORKDIR name def: APP
TARGET_ENV_COPYD <dir name> <folder path in target> - Copy local folder to docker image
TARGET_ENV_COPYF <file name> <file path in target> - Copy local file to docker image
TARGET_ENV_COPY <local target name> <local target path in target> - Copy local object to docker image (Recomended over COPYD and COPYF. (To copy .sh files that you plan to run on image use COPYE) )
TARGET_ENV_COPYE <local sh name> <file path in target> <true/false> <true/false> - Copy local sh file to docker image. Check for #! on the begining?. Run chmod +x on file?
TARGET_ENV_INITFILE <sh file name in target> - Set default sh file on image
TARGET_ENV_SHAREFOLDER <local folder name> <target folder name> - Add access to folder from docker (-v flag)
